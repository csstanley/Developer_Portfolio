import { Button } from 'react-bootstrap';
import './port-btn.css';

class PortBtn extends Component {
    render() {
        return (
            <Button variant="dark">Portfolio</Button>
        )
    }
}

export default PortBtn;